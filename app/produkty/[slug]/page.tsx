import { notFound } from 'next/navigation';
import Link from 'next/link';
import { getProduct, formatPrice, products } from '@/lib/products';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}));}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)notFound();return <main className="page"><div className="container"><div className="detail"><div><div className="eyebrow">{p.type==='freebie'?'DARMOWY MATERIAŁ':p.type==='service'?'USŁUGA':'E-BOOK'}</div><h1 className="page-title" style={{fontSize:'clamp(42px,6vw,68px)',letterSpacing:'-.06em',lineHeight:1,marginTop:12}}>{p.name}</h1><p className="muted" style={{fontSize:18,lineHeight:1.55}}>{p.description}</p></div><aside className="detail-card"><div className="eyebrow">DLA KOGO</div><p>{p.audience}</p><div className="price">{formatPrice(p.price)}</div><Link className="button" href={p.price===0?'/rejestracja':'/rejestracja'}>{p.price===0?'Odbierz materiał':'Kup / zamów'}</Link><p className="muted" style={{fontSize:12,marginTop:14}}>Finalna obsługa płatności zostanie podłączona po konfiguracji operatora.</p></aside></div><section className="section" style={{paddingBottom:0}}><h2>Co otrzymujesz</h2><ul>{p.includes.map(item=><li key={item}>{item}</li>)}</ul></section></div></main>}
